This is a convenient manager for creating and organizing notes, which saves data in a Postgres database and allows you to create backups.

For configuration:
```
cmake ..
```
For build:
```
cmake --build .
```
For start program:
```
./start
```